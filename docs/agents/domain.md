# Domain Docs

本仓库采用 single-context 领域文档布局。

## 探索前读取

- 根目录 `CONTEXT.md`，如存在。
- `docs/adr/` 中与当前工作相关的架构决策，如存在。

文件不存在时直接继续，不预先创建空文件。领域术语或架构决策实际形成时，再由对应工作流创建。

输出中涉及领域概念时，应使用 `CONTEXT.md` 定义的词汇。若实现方案与现有 ADR 冲突，必须明确指出。
