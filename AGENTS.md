# 工程规则

## 执行规则
- **保持简洁**：所有回复必须简洁直接，避免冗余解释
- **歧义确认**：当指令存在歧义、模糊、信息缺失或可能多解时，必须逐条列出所有合理可能性并要求确认，在明确选择前不执行任何操作
- 不要过度工程化，不要用checksum锁定一些无意义的东西，不要预想不存在的极限场景
- 不要频繁跑全量测试，如果要跑全量测试必须使用多线程，永远不准单线程
- 不要做过度兜底设计
- 不要过度防御性编程
- 永远不要使用Worktree
