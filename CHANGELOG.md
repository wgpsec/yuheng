# 更新日志

玉衡的版本变化记录在这里。版本号以应用安装包显示的版本为准。

## 0.3.4 - 看板删除与启动修复

- 空任务看板可以直接删除；看板有任务时仍需选择迁移目标，删除默认空看板会自动保留有效默认入口。
- 删除看板确认弹窗适配深色、浅色和 Notion 主题，统一迁移选择框与操作按钮样式。
- 恢复 macOS 开发启动脚本及 Electron 构建配置，修复 npm run dev 缺失导致的启动失败。

## 0.3.3 - 安全迁移与启动恢复

- 数据库升级前会创建并校验原始快照，迁移历史、完整性和版本兼容性均在正常工作区启动前验证。
- 数据库迁移或初始化失败时进入独立恢复窗口，可重试启动、恢复已验证快照、导出脱敏诊断或打开数据目录。
- 恢复操作会先隔离失败数据库，快照损坏、版本过高或迁移账本异常时拒绝覆盖现有数据。
- 主进程 IPC、运行协调、窗口和关闭生命周期按领域拆分，恢复模式不加载普通业务能力。
- 存储访问迁入共享连接和事务边界下的领域 Repository，AppStore 不再隐式建表或迁移。
- Renderer 状态与样式按功能域拆分，并阻止旧会话 Run 事件污染当前会话。

## 0.3.2 - 块编辑与桌面 Pet 增强

- 支持从左侧沟槽连续选择多个内容块，并提供独立的批量操作栏。
- 批量复制会将所选块的 Markdown 写入系统剪贴板，不再在当前页面创建副本。
- 页面内创建副本改为独立命令，并补充复制成功、失败和重试反馈。
- 桌面 Pet 增加可行动状态反馈、任务提醒和精确导航，完成、失败与审批提示可直接定位相关内容。
- Pet 设置增加防打扰偏好、皮肤兼容性诊断和受管导入、删除、Finder 定位能力。
- Pet 支持多显示器位置记忆、边缘吸附、可选惯性与回弹，并在全屏和隐藏状态下停止非必要动画。

## 0.3.1 - 安全审批与会话权限

- 会话输入框新增谨慎、智能审批和完全访问三种工具权限模式。
- 权限策略由主进程统一执行，并在每次运行开始时固定权限快照。
- 完全访问仅在当前应用会话有效，非法参数、隔离边界和安全审计仍不可绕过。
- Electron Renderer、IPC、路径访问和敏感工具输入增加纵深安全保护。

## 0.3.0 - 笔记知识与编辑可靠性

- 桌面 Pet 增强：增加 thinking、attention、error 等运行状态，隐藏窗口时暂停动画和计时器。
- Pet 设置支持位置锁定、不透明度和始终置顶，并增加独立窗口右键控制菜单。
- Codex Pet manifest 可按状态配置精灵行和帧时长，旧皮肤继续使用默认兼容配置。
- Pet 状态气泡会显示当前运行阶段和工具名称，点击可定位到对应会话，并正确处理并发运行与主窗口冷启动。
- 笔记支持稳定的页面内部链接、反向链接和 [[ 页面选择器，标题修改或页面移动后链接仍然有效。
- 侧栏页面菜单支持复制页面链接，粘贴到正文后会自动解析为可点击的笔记链接。
- 笔记 AI 改写、总结和扩展改为差异预览，用户确认后应用，并支持撤销和过期内容保护。
- 自动保存采用按页面隔离的有序队列，旧响应不会覆盖新编辑，失败保存可显式重试。
- 异常退出可在用户确认后恢复本地草稿；完整备份恢复会同步重映射笔记内部链接并保留未知链接。

## 0.2.1 - 页面与看板 AI 助手

- 笔记页面和任务看板新增右下角 AI 快速入口，可在当前上下文中直接发起对话。
- AI 抽屉支持上下文边界控制、多轮短对话、即时处理中反馈和停止请求。
- 笔记子页面可作为编辑器块移动和打开，并修复页面块渲染稳定性问题。

## 0.2.0 - 玉衡工作台

- 整合 Profile、任务看板、备份恢复和桌面宠物能力，形成完整的本地 Agent 工作台。
- 会话与任务工作流支持更细致的拖拽、编辑、筛选和状态反馈。
- 支持从历史消息创建独立的会话分支，应用设置和备份信息显示当前版本。
- 桌面宠物支持 Codex Pet 皮肤、动画状态、拖拽定位和独立管理。

## 0.1.9 - Codex Pet 皮肤兼容

- 支持自动扫描 ~/.codex/pets 中的 Codex Pet v1/v2 皮肤包。
- 新增宠物皮肤管理、缩略图预览、刷新和打开目录操作。
- 修复精灵动画快速播放、空白帧和点击回到主窗口的问题。

## 0.1.8 - 桌面宠物

- 新增可拖拽的桌面宠物窗口，并支持唤醒、关闭和状态提醒。
- 设置页支持宠物开关、桌面显示偏好和宠物大小持久化。
- 宠物点击可快速回到玉衡主窗口，拖拽位置跨窗口操作保持稳定。

## 0.1.7 - 任务工作台优化

- 任务卡片和看板支持拖拽排序、跨看板移动/复制及快捷编辑。
- 任务正文升级为块编辑器，支持 Markdown 粘贴和更清晰的卡片预览。
- 统一工作台排版规范，优化对话消息布局、工具记录折叠和交互动画。

## 0.1.6 - 备份与界面动效

- 新增包含会话、任务、附件、运行记录和 Agent 上下文的全量备份与恢复。
- 备份归档增加路径校验、完整性校验和失败回滚保护。
- 补充侧边栏、工作区、预览和审批面板的过渡动画。

## 0.1.5 - Profile 与运行时

- 新增助手、分析师和审计专家三种对话 Profile。
- 助手 Profile 自动注入可信的本地日期时间上下文，其他 Profile 默认不注入。
- 推理级别按会话独立保存，并增强 Provider 连接测试、会话恢复和版本信息展示。

## 0.1.4 - 上下文窗口可见与可配置

- 会话顶栏显示当前上下文占用、窗口容量和使用进度。
- 模型服务设置支持自定义上下文窗口，默认值调整为 200K tokens。
- 会话恢复与新一轮完成后会保留并更新最近的上下文使用数据。

## 0.1.3 - 任务与会话体验

- 新增本地任务看板、任务类型、截止日期、提醒和附件管理。
- Agent Runtime 可以查询、创建和更新任务，并保留来源会话关联。
- 推理级别改为按会话独立保存，重新打开会话后会恢复原选择。

## 0.1.2 - Computer Use

- 新增可选的 Computer Use，支持桌面控制和受控浏览器操作。
- Browser Use 与 Computer Use 互斥启用，避免两套浏览器控制能力同时运行。
- Computer Use 截图作为运行产物保存，并在工具执行记录中展示。

## 0.1.1 - Browser Use

- 新增默认关闭、按需启用的 Browser Use 浏览器自动化能力。
- 浏览器截图可在对话的工具执行记录中查看，并支持应用重启后继续访问。
- 点击、填写等敏感操作增加逐次审批，工具记录对敏感输入和截图数据做脱敏处理。

## 0.1.0 - 首个桌面版本

- 提供基于 Electron 和 React 的 macOS 桌面客户端。
- 接入 Pi Agent Runtime，支持 Provider 配置、会话管理和流式响应。
- 会话、消息与运行记录保存在本地 SQLite 数据库中。
